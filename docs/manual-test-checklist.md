# Ручная проверка Datamatrix Notepad

Дата подготовки: 2026-07-25. Обновлено: 2026-09-07 для проверки сохранения GS.

Этот документ отделяет автоматические доказательства от проверок, которым необходимы Chrome/Edge, GitHub Pages или реальный сканер. Незакрытый пункт блокирует production-релиз, но не скрывает уже подтверждённую автоматикой функциональность.

## 1. Среда проверки

- [ ] URL или `localhost`: ______________________________
- [ ] Коммит: ___________________________________________
- [ ] Windows: __________________________________________
- [ ] Chrome и версия: __________________________________
- [ ] Edge и версия: ____________________________________
- [ ] Модель сканера: ___________________________________
- [ ] Фактические параметры порта: _______________________
- [ ] Фактическая кодировка входного потока: ______________
- [ ] Фактический суффикс: _______________________________

## 2. Автоматические доказательства

На 2026-09-07 локально подтверждено:

- [x] `dotnet test ".\Datamatrix Notepad.slnx" --configuration Release`: 81/81 тест.
- [x] Release-сборка завершилась без ошибок и предупреждений.
- [x] Финальный `dotnet format ... --verify-no-changes` прошёл.
- [x] `dotnet list ... package --vulnerable --include-transitive` не нашёл уязвимых NuGet-пакетов.
- [x] `node --check` прошёл для `serial.js`, `storage.js` и `download.js`.
- [x] Node `TextDecoder("utf-16be")` декодировал разделённые между chunks байты `00 30 00 | 31 00 0D 00 0A` как `01\r\n`.
- [x] Release publish сформировал `wwwroot`, `_framework` и статические ресурсы.
- [x] Тестовый Pages-артефакт содержит base href `/datamatrix-notepad/`, `404.html` и `.nojekyll`.
- [x] Автоматические тесты покрывают CR/LF/CRLF, разделённый суффикс, несколько и повторные коды, отсутствие активной заметки, все примеры экспортных имён и точный текст запроса на скачивание.

## 3. Браузер и интерфейс

Повторить отдельно в Chrome и Edge.

- [ ] Главная страница открывается без необработанных ошибок в Console.
- [ ] Страница настроек открывается без необработанных ошибок в Console.
- [ ] При ширине 1280 px основные действия и редактор видимы без горизонтальной прокрутки.
- [ ] При ширине 600 px основные действия складываются в одну колонку без горизонтальной прокрутки.
- [ ] Все поля имеют видимые подписи; Tab проходит элементы в логичном порядке.
- [ ] Focus indicator виден на ссылках, полях и кнопках.
- [ ] Skip-link появляется при первом нажатии Tab и переводит focus к содержимому.
- [ ] Escape закрывает диалог удаления.
- [ ] Tab и Shift+Tab остаются внутри открытого диалога.
- [ ] После отмены focus возвращается на «Удалить заметку».
- [ ] После удаления focus переходит к удалению следующей заметки либо к «Новая заметка».
- [ ] При отсутствии `navigator.serial` показано понятное сообщение; фиктивного подключения нет.
- [ ] Ошибки настройки, сохранения, подключения и скачивания показаны рядом с соответствующим действием.

## 4. Хранение и заметки

- [ ] Создать две заметки; после reload названия, тексты и активная заметка восстановились.
- [ ] Изменить поле и сразу закрыть/перезагрузить вкладку; последнее изменение сохранилось через `pagehide`.
- [ ] Пустое название показано в списке как «Без названия».
- [ ] Отмена удаления не меняет заметку.
- [ ] Подтверждение удаляет только выбранную заметку.
- [ ] После удаления активной заметки выбрана соседняя; после удаления последней показано пустое состояние.
- [ ] В сохранённом JSON есть VID/PID и параметры, но нет объекта `SerialPort`.

## 5. Web Serial и реальный сканер

- [ ] Chrome определяет Web Serial и открывает picker только после нажатия кнопки.
- [ ] Edge определяет Web Serial и открывает picker только после нажатия кнопки.
- [ ] Отмена picker показывает нейтральное сообщение и не повреждает заметки.
- [ ] Порт открывается с фактическими настройками сканера.
- [x] Для текущего сканера выбрана UTF-16BE; после переподключения код отображается без промежутков и `U+0000`.
- [ ] После reload настройка UTF-16BE восстановилась и применяется при повторном подключении.
- [ ] Диагностический лог показывает псевдоним, VID/PID при наличии, сырые данные и завершённый код.
- [ ] CRLF отображается как `\r\n`.
- [ ] GS отображается как `\u001D` в `[RAW]` и `[CODE]`; заметка и загруженный файл сохраняют сам `U+001D`.
- [ ] Код, разбитый между несколькими чтениями, добавляется один раз и полностью.
- [ ] Десять последовательных сканов дают ровно десять отдельных строк.
- [ ] Повтор одного кода добавляет дубликат отдельной строкой.
- [ ] Без активной заметки показано предупреждение; новая заметка не создаётся.
- [ ] Скан на странице настроек не меняет заметку.
- [ ] Переходы Settings → Home → Settings не создают дублирующихся обработчиков.
- [ ] Отключение USB показывает состояние отключения/ошибку без повреждения заметок.
- [ ] Повторное подключение работает.
- [ ] После reload ровно один подходящий разрешённый порт подключается повторно.
- [ ] При нуле или нескольких совпадениях приложение требует ручной picker.

## 6. Экспорт

Проверить имя, MIME/открытие как текст и побайтовое UTF-8 содержимое:

- [ ] `Партия 00ЦБ-123456 от 25.07.2026` → `123456;25.07.2026.txt`.
- [ ] `Партия 00ЦБ-123456` → `123456;.txt`.
- [ ] `От 25.07.2026` → `;25.07.2026.txt`.
- [ ] `Произвольное название` → `;.txt`.
- [ ] Кириллица, переводы строк и DataMatrix-коды в файле побайтно совпадают с заметкой.
- [ ] Повторные скачивания работают; в Console нет ошибок.
- [ ] Экспорт не меняет заметку, статус сохранения и serial-соединение.

## 7. GitHub Pages

Выполнять только после явного разрешения на production deploy.

- [ ] В Settings → Pages выбран источник GitHub Actions.
- [ ] Workflow quality gates и deploy завершились успешно.
- [ ] Production URL использует HTTPS.
- [ ] Главная страница открывается по production URL.
- [ ] Прямой URL `/settings` открывается и переживает reload.
- [ ] Главная страница переживает reload.
- [ ] `_framework` и остальные ресурсы загружаются без 404.
- [ ] В артефакте нет исходников, секретов и пользовательских данных.
- [ ] Предыдущий успешный workflow/коммит записан как точка отката: __________________.

## 8. Критерии готовности MVP

| № | Критерий спецификации | Доказательство | Статус |
|---:|---|---|---|
| 1 | Release-сборка и тесты проходят | Финальные 71/71 и Release build | автоматизировано |
| 2 | Chrome и Edge определяют Web Serial | Раздел 5 | ожидает ручной проверки |
| 3 | Порт выбирается и открывается с параметрами | T3/T7 + раздел 5 | ожидает сканер |
| 4 | Диагностика показывает скан и control chars | T3 + раздел 5 | ожидает сканер |
| 5 | Разделённый CRLF завершает код | `ScanFrameParserTests` | автоматизировано |
| 6 | Десять сканов дают десять строк | T8 + раздел 5 | ожидает сканер |
| 7 | Создание и изменения сохраняются | `NoteStateTests`; раздел 4 | ожидает browser reload |
| 8 | Удаление требует подтверждения | T6; разделы 3–4 | ожидает keyboard/browser |
| 9 | Reload восстанавливает данные | T4/T7; раздел 4 | ожидает browser reload |
| 10 | Все экспортные имена верны | `ExportFileNameTests` | автоматизировано |
| 11 | UTF-8 файл совпадает с заметкой | `NoteExportServiceTests`; раздел 6 | ожидает browser bytes |
| 12 | Ошибки не повреждают заметки | тесты границ + разделы 3–5 | частично, ожидает устройство |
| 13 | GitHub Pages и маршруты работают | T12 + раздел 7 | ожидает deploy |
| 14 | Пройдена аппаратная проверка | Раздел 5 | не выполнено |
| 15 | Пользователь одобрил итог | Подтверждение пользователя | не выполнено |

## 9. Решение о выпуске

- [ ] Все обязательные пункты выше выполнены.
- [ ] Пользователь одобрил итоговое поведение.
- [ ] Решение: `GO` / `NO-GO`.
- [ ] Проверил: ______________________
- [ ] Дата: __________________________

Если после deploy обнаружена критическая ошибка, откатить Pages на предыдущий проверенный коммит/успешный workflow. Сохранённые данные схемы `v1` не удалять автоматически.
